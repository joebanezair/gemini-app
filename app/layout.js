import './style.css';
export const metadata={title:'JobSwipe AI',description:'Swipe fresh job leads and send tailored applications'};
export default function RootLayout({children}){return <html><body>{children}</body></html>}
